apiVersion: v1
kind: Service
metadata:
  name: cicd-demo
  namespace: production
  labels:
    app: cicd-demo
    environment: production
spec:
  type: NodePort
  selector:
    app: cicd-demo

  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080
